import { getPlatformService } from "@/server/src/platform/service";export const dynamic="force-dynamic";export async function GET(){return Response.json({integrations:await getPlatformService().integrations()})}
