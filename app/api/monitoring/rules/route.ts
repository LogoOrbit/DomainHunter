import { getOpportunityMonitoringService } from "@/server/src/monitoring/service"; export const dynamic = "force-dynamic"; export async function GET() { return Response.json({ rules: await getOpportunityMonitoringService().rules() }); } export async function POST(request: Request) { const body = await request.json() as { domainId?: string }; if (!body.domainId) return Response.json({ error: "domainId is required" }, { status: 400 }); return Response.json({ rule: await getOpportunityMonitoringService().rule({ ...body, domainId: body.domainId }) }); }
