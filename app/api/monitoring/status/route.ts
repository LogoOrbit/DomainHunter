import { getOpportunityMonitoringService } from "@/server/src/monitoring/service"; export const dynamic = "force-dynamic"; export async function GET(request: Request) { return Response.json({ monitoring: await getOpportunityMonitoringService().status(new URL(request.url).searchParams.get("id") ?? undefined) }); }
