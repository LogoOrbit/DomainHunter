import { getOpportunityMonitoringService } from "@/server/src/monitoring/service"; export const dynamic = "force-dynamic"; export async function GET(request: Request) { const p = new URL(request.url).searchParams; return Response.json({ opportunities: await getOpportunityMonitoringService().opportunities({ domainId: p.get("domainId") ?? undefined, priority: p.get("priority") ?? undefined, take: Number(p.get("take") ?? 25), cursor: p.get("cursor") ?? undefined }) }); }
