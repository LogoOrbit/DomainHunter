import { getResearchWorkspaceService } from "@/server/src/workspace/service"; export async function POST(request: Request, context: { params: Promise<{ id: string }> }) { const { companyId } = await request.json() as { companyId?: string }; if (!companyId) return Response.json({ error: "companyId is required" }, { status: 400 }); return Response.json({ item: await getResearchWorkspaceService().addCompany((await context.params).id, companyId) }, { status: 201 }); }
