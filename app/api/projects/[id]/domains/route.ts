import { getResearchWorkspaceService } from "@/server/src/workspace/service"; export async function POST(request: Request, context: { params: Promise<{ id: string }> }) { const { domainId } = await request.json() as { domainId?: string }; if (!domainId) return Response.json({ error: "domainId is required" }, { status: 400 }); return Response.json({ item: await getResearchWorkspaceService().addDomain((await context.params).id, domainId) }, { status: 201 }); }
