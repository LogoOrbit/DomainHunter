import { getResearchWorkspaceService } from "@/server/src/workspace/service"; export const dynamic = "force-dynamic";
export async function GET() { return Response.json({ projects: await getResearchWorkspaceService().projects() }); }
export async function POST(request: Request) { const body = await request.json() as { name?: string; description?: string }; if (!body.name?.trim()) return Response.json({ error: "name is required" }, { status: 400 }); return Response.json({ project: await getResearchWorkspaceService().createProject({ name: body.name, description: body.description }) }, { status: 201 }); }
