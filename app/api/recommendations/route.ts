import { getOpportunityMonitoringService } from "@/server/src/monitoring/service"; export const dynamic = "force-dynamic"; export async function GET(request: Request) { const p = new URL(request.url).searchParams; return Response.json({ recommendations: await getOpportunityMonitoringService().recommendations({ domainId: p.get("domainId") ?? undefined, take: Number(p.get("take") ?? 25) }) }); }
